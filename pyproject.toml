[project]
name = "ha-inlite"
version = "0.2.0"
description = "Home Assistant custom integration for in-lite outdoor lighting"
requires-python = ">=3.12"
license = "MIT"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["custom_components/inlite/lib"]
